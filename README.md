# Royal Lab

Windows 皇室战争离线人机训练控制台，通过 ADB 连接 MuMu 或其他本地模拟器。

## 开始使用

1. 双击 `启动控制台.vbs`。
2. 点击“连接并检测”；其他模拟器可用“自定义 ADB”填写端口。
3. 首次使用：进入离线人机页面，通过“工具 → 画面标定”保存离线标志。
4. 选择运行模型，点击“离线训练”；点击“安全停止”结束。

主界面显示设备、离线校验、完成局数、实时画面和日志。画面标定、训练数据、精确标注、无标注自学、示范学习和双机同步统一放在“工具”菜单。

快捷键：`F5` 刷新画面，`Ctrl+Enter` 开始，`Esc` 安全停止。

运行版本从 V5 到 M3-C3，选择保存在本机，下次启动训练时生效。程序仅运行已验证的离线人机对局。

## 数据与训练

- **自动采集**：对局记录保存在 `runs/`，示范操作保存在 `demonstrations/`。
- **双机共享**：每台电脑首次运行 `setup_sync.bat`，重启控制台后自动同步；手动同步在“工具”菜单。
- **模型**：候选上传与实战启用分开；质量不合格的候选可同步，但不会自动晋级。
- **GPU**：NVIDIA 电脑运行 `setup_gpu.bat`，验证后重启控制台。

## 参考

- [使用、策略与训练命令](docs/usage.md)
- [双机训练共享](docs/training-sharing.md)
- [GPU 设置与验证](docs/gpu.md)

日常启动也可使用 `start_ui.bat`。运行测试：`run_tests.bat`，或 `python -m unittest discover -s tests -v`。依赖见 `requirements.txt`，启动脚本会自动安装缺少的基础依赖。
